```markdown
# 🟢 JavaScript/Node.js Build & Package

## 🎯 JS Backend Build (No Special Artifact)
```
**npm/Yarn** = Package Managers (Dependencies ONLY)
- **NO transpiling/compiling** → Raw JS files + package.json
- Artifact = **ZIP/TAR** (app code **WITHOUT** node_modules)
- `node_modules` = **REBUILT** on target server
```

## 📦 Backend Package.json Workflow
```bash
# 1. DEV - Install deps
npm install                    # → node_modules + package-lock.json
yarn install                   # → yarn.lock

# 2. BUILD (just copy)
npm run build          # Usually: zip src/ + package.json
zip -r app.zip src/ package.json

# 3. DEPLOY → Server
ssh server
unzip app.zip
npm install              # Fresh node_modules
npm start                # node server.js
```

## 🚀 Backend Run Commands
```bash
npm start      # package.json → "start": "node server.js"
npm run dev    # "dev": "nodemon server.js"
npm test       # Run tests
npm publish    # Publish to npm registry
```

## 🎨 Frontend Build (React/Vue/Angular)
```
**BROWSER NEEDS**: Transpiled + Minified JS (no JSX/ES6+)
**Webpack/Vite** = Build tools (transpile + bundle)
**npm/Yarn** = Still manages dependencies
```

## 📁 Frontend vs Backend package.json

| Setup | Pros | Cons |
|-------|------|------|
| **SEPARATE** `frontend/package.json` + `backend/package.json` | Clear separation | 2x installs |
| **MONOREPO** `package.json` (workspaces) | Single `npm install` | Complex scripts |

```json
// Frontend package.json
{
  "scripts": {
    "build": "webpack --mode production",
    "start": "webpack serve"
  }
}

// Backend package.json  
{
  "scripts": {
    "start": "node server.js"
  }
}
```

## 🔨 Frontend Build Commands
```bash
# React + Create React App
npm run build    # → build/ (static HTML/JS/CSS)

# Vite (Faster)
npm run build    # → dist/

# Custom Webpack
npm run build    # Bundle → dist/bundle.js
```

## 🌐 Fullstack Examples

```bash
# 1. JS Frontend + Node Backend (Common)
frontend/   → npm run build → build/
backend/    → npm run build → dist/
Final: nginx serves build/ + backend API

# 2. React Frontend + Java Backend (ENTERPRISE)
React → npm run build → static/
Java → mvn package → app.war (contains static/)
Deploy: Tomcat → app.war

# 3. React + Python Flask
React → npm run build → static/
Flask → gunicorn app:app (serves static/)
```

## 🔥 Complete Deployment Flow
```bash
# BACKEND NODE
1. zip -r backend.zip backend/ package.json
2. scp backend.zip server:
3. ssh server "unzip backend.zip && cd backend && npm i && npm start"

# FRONTEND REACT
1. cd frontend && npm run build
2. cp -r build/ ../backend/src/main/resources/static/
3. cd ../backend && mvn package → app.war (Frontend+Bundled!)
```

## 💡 Pro Tips
```
✅ Backend: ZIP(code) + package.json → npm install on server
✅ Frontend: ALWAYS transpile (npm run build)
✅ WAR file = Frontend static + Java backend (Enterprise)
✅ Monorepo workspaces = Single npm install for all
✅ Docker = Ultimate solution (all platforms)
```
```