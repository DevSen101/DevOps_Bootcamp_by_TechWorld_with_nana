```markdown
# 🌍 Build Tools - ALL Languages (Universal Pattern)

## 🎯 UNIVERSAL BUILD PATTERN
```
1. **CONFIG FILE** → Dependencies + Build instructions
2. **CLI TOOL** → install/test/build/package/start  
3. **REPO** → Central dependency storage
4. **ARTIFACT** → Deployable package
```

## 📋 Complete Language Matrix

| Language | Config File | Package Manager | Build Tool | Repo | Artifact | Run Command |
|----------|-------------|----------------|------------|------|----------|-------------|
| **Java** | `pom.xml` / `build.gradle` | Maven/Gradle | Maven/Gradle | Maven Central | JAR/WAR | `java -jar app.jar` |
| **Node.js** | `package.json` | npm/Yarn | Webpack(none) | npm registry | ZIP | `npm start` |
| **Python** | `pyproject.toml` / `setup.py` | pip/Poetry | setuptools | PyPI | wheel | `python -m app` |
| **.NET** | `.csproj` | NuGet | dotnet CLI | NuGet.org | nupkg | `dotnet MyApp.dll` |
| **Go** | `go.mod` | Go Modules | go build | pkg.go.dev | binary | `./myapp` |
| **Rust** | `Cargo.toml` | Cargo | cargo build | crates.io | binary | `./myapp` |
| **PHP** | `composer.json` | Composer | none | Packagist | ZIP | `php artisan serve` |

## 🚀 Command Pattern (Universal)

```bash
# 1. Install Dependencies
npm install     # JS
pip install -r requirements.txt  # Python
go mod tidy     # Go
cargo build     # Rust

# 2. Build/Test
npm run build   # JS Frontend
mvn package     # Java
pip install .   # Python wheel
go build        # Go binary

# 3. Run
npm start       # JS
java -jar app.jar  # Java
python app.py   # Python
./myapp         # Go/Rust
```

## 🏪 Universal Repositories
```
Java: Maven Central, JCenter
JS: npm, Yarn (npm)
Python: PyPI
.NET: NuGet.org
Go: pkg.go.dev
Rust: crates.io
PHP: Packagist
```

## 💡 CORE CONCEPTS (All Languages)
```
✅ **Dependency File** = Lock versions (package-lock.json, pom.xml)
✅ **Local Install** = Dev workflow (npm i, pip install -e .)  
✅ **Production** = Fresh install from lockfile
✅ **Artifact Repo** = Nexus/JFrog (store built packages)
✅ **Immutable** = Never change deployed artifact
✅ **Reproducible** = Same config = same output
```

## 🎯 Pro Summary
```
**Package Manager** = Downloads dependencies (npm/pip/nuget)
**Build Tool** = Compiles + Packages (Maven/Gradle/Webpack)
**Artifact** = Single deployable file (JAR/wheel/binary)
**CI/CD** = Automated: checkout → build → test → store → deploy
```
```